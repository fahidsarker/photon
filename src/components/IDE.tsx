"use client";
import { LanguageInterface } from '@/models/language';
import React from 'react'

const IDE = ({ }: { language: LanguageInterface }) => {
    return (
        <div>IDE</div>
    )
}

export default IDE